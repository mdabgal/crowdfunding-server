/**
 * =============================================================================
 * Contribution Routes
 * =============================================================================
 *
 * Endpoints for campaign contributions / pledges.
 *
 * TODO: Wire controller methods when contribution features are built.
 *
 * Planned routes:
 *   POST   /api/v1/contributions               → Create contribution
 *   GET    /api/v1/contributions                → List user contributions
 *   GET    /api/v1/contributions/:id            → Get contribution by ID
 *   GET    /api/v1/contributions/campaign/:id   → List contributions for a campaign
 */

import { Router } from 'express';

const router = Router();

// Routes will be defined here

export default router;
