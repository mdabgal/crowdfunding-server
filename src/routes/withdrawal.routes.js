/**
 * =============================================================================
 * Withdrawal Routes
 * =============================================================================
 *
 * Endpoints for fund withdrawal requests and processing.
 *
 * TODO: Wire controller methods when withdrawal features are built.
 *
 * Planned routes:
 *   POST   /api/v1/withdrawals          → Request withdrawal
 *   GET    /api/v1/withdrawals          → List user withdrawals
 *   GET    /api/v1/withdrawals/:id      → Get withdrawal by ID
 *   PATCH  /api/v1/withdrawals/:id      → Approve / reject withdrawal (admin)
 */

import { Router } from 'express';

const router = Router();

// Routes will be defined here

export default router;
